import { Journal } from "./journal.js";
import { Logger } from "./logger.js";
import * as http$1 from "node:http";

//#region src/moderation.d.ts

interface ModerationResult {
  flagged: boolean;
  categories: Record<string, boolean>;
  category_scores?: Record<string, number>;
}
interface ModerationFixture {
  match: string | RegExp;
  result: ModerationResult;
}
declare function handleModeration(req: http$1.IncomingMessage, res: http$1.ServerResponse, raw: string, fixtures: ModerationFixture[], journal: Journal, defaults: {
  logger: Logger;
}, setCorsHeaders: (res: http$1.ServerResponse) => void): Promise<void>;
//# sourceMappingURL=moderation.d.ts.map

//#endregion
export { ModerationFixture, ModerationResult, handleModeration };
//# sourceMappingURL=moderation.d.ts.map