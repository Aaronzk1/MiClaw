import { type CompactionMetadata } from "./compaction-metadata.js";
import type { ToolExecutionInput } from "../types.js";
export declare function rewriteGitStatusLines(lines: string[]): string[];
export declare function rewriteSearchLines(lines: string[], noOmit?: boolean): {
    lines: string[];
    compaction?: CompactionMetadata;
};
export declare function rewriteGitDiffLines(lines: string[], noOmit?: boolean): {
    lines: string[];
    compaction?: CompactionMetadata;
};
export declare function rewriteGhLines(lines: string[], input: ToolExecutionInput, noOmit?: boolean): {
    lines: string[];
    compaction?: CompactionMetadata;
};
