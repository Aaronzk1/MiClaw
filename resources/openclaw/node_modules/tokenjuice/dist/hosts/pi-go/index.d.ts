export type PiGoSkillOptions = {
    projectDir?: string;
};
export type InstallPiGoSkillResult = {
    skillPath: string;
    backupPath?: string;
};
export type UninstallPiGoSkillResult = {
    skillPath: string;
    removed: boolean;
};
export type PiGoDoctorReport = {
    skillPath: string;
    hasTokenjuiceMarker: boolean;
    status: "ok" | "warn" | "broken" | "disabled";
    issues: string[];
    advisories: string[];
    fixCommand: string;
    checkedPaths: string[];
    missingPaths: string[];
};
export declare function installPiGoSkill(skillPath?: string, options?: PiGoSkillOptions): Promise<InstallPiGoSkillResult>;
export declare function uninstallPiGoSkill(skillPath?: string, options?: PiGoSkillOptions): Promise<UninstallPiGoSkillResult>;
export declare function doctorPiGoSkill(skillPath?: string, options?: PiGoSkillOptions): Promise<PiGoDoctorReport>;
