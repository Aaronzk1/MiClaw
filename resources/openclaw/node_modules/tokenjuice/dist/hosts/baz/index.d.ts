export type BazSkillOptions = {
    projectDir?: string;
};
export type InstallBazSkillResult = {
    skillPath: string;
    backupPath?: string;
};
export type UninstallBazSkillResult = {
    skillPath: string;
    removed: boolean;
};
export type BazDoctorReport = {
    skillPath: string;
    hasTokenjuiceMarker: boolean;
    hasUnsafePathIssue: boolean;
    status: "ok" | "warn" | "broken" | "disabled";
    issues: string[];
    advisories: string[];
    fixCommand: string;
    checkedPaths: string[];
    missingPaths: string[];
};
export declare function installBazSkill(skillPath?: string, options?: BazSkillOptions): Promise<InstallBazSkillResult>;
export declare function uninstallBazSkill(skillPath?: string, options?: BazSkillOptions): Promise<UninstallBazSkillResult>;
export declare function doctorBazSkill(skillPath?: string, options?: BazSkillOptions): Promise<BazDoctorReport>;
