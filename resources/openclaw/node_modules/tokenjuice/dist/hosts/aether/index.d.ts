export type AetherPromptOptions = {
    projectDir?: string;
};
export type InstallAetherPromptResult = {
    promptPath: string;
    settingsPath: string;
    backupPath?: string;
    settingsBackupPath?: string;
    agentsUpdated: number;
};
export type UninstallAetherPromptResult = {
    promptPath: string;
    settingsPath: string;
    removed: boolean;
    promptsRemoved: number;
};
export type AetherDoctorReport = {
    promptPath: string;
    settingsPath: string;
    hasTokenjuiceMarker: boolean;
    status: "ok" | "broken" | "disabled";
    issues: string[];
    advisories: string[];
    fixCommand: string;
    checkedPaths: string[];
    missingPaths: string[];
};
export declare function installAetherPrompt(promptPath?: string, options?: AetherPromptOptions): Promise<InstallAetherPromptResult>;
export declare function uninstallAetherPrompt(promptPath?: string, options?: AetherPromptOptions): Promise<UninstallAetherPromptResult>;
export declare function doctorAetherPrompt(promptPath?: string, options?: AetherPromptOptions): Promise<AetherDoctorReport>;
