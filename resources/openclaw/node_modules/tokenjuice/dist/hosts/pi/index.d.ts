export type PiExtensionCommandOptions = {
    local?: boolean;
    binaryPath?: string;
    nodePath?: string;
};
export type InstallPiExtensionResult = {
    extensionPath: string;
    backupPath?: string;
};
export type UninstallPiExtensionResult = {
    extensionPath: string;
    backupPath?: string;
    removed: boolean;
    restoredBackup: boolean;
};
export type PiDoctorReport = {
    extensionPath: string;
    status: "ok" | "warn" | "broken" | "disabled";
    issues: string[];
    fixCommand: string;
    checkedPaths: string[];
    missingPaths: string[];
};
export declare function installPiExtension(extensionPath?: string, options?: PiExtensionCommandOptions): Promise<InstallPiExtensionResult>;
export declare function uninstallPiExtension(extensionPath?: string): Promise<UninstallPiExtensionResult>;
export declare function doctorPiExtension(extensionPath?: string): Promise<PiDoctorReport>;
